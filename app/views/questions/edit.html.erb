<div class="question-page">

  <div class="question-detail-card">

    <div class="question-detail-header">
      <span class="question-label">
        <%= t("edit_question.label") %>
      </span>
    </div>

    <h1><%= t("edit_question.title") %></h1>

    <%= form_with model: @question do |form| %>

      <div class="form-group">
        <%= form.label :description, t("edit_question.question") %>

        <%= form.text_area :description,
            rows: 6 %>
      </div>

      <div class="form-group">
        <%= form.label :category_id, t("edit_question.category") %>

        <%= form.collection_select :category_id,
            @categories,
            :id,
            :name,
            { prompt: t("edit_question.select_category") } %>
      </div>

      <div class="question-actions">

        <%= form.submit t("edit_question.update"),
            class: "submit-answer-button" %>

        <%= link_to t("edit_question.cancel"),
                    question_path(@question),
                    class: "action-button" %>

      </div>

    <% end %>

  </div>

</div>